# Секреты и локальная конфигурация (CLAUDE.md §5: в репо только .env.example).
.env
.env.*
!.env.example
*.key
*.pem
secrets/
# Секреты прошивок ESPHome (в репо — только secrets.yaml.example).
firmware/esphome/secrets.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Docker / runtime
*.pid
*.log

# Артефакты видеоаналитики (пишутся в рантайме на общий том)
/data/artifacts/

# Рантайм-ассеты контура (объектные, не в репозитории)
models/*.task
config/schedules.json
# Вендоренные веб-ассеты MediaPipe/hls/модель для live.html (качает fetch_web_assets.sh)
services/api-gateway/api_gateway/static/vendor/
# Реальный конфиг объекта для сидинга (адреса камер и т.п.); в репо — только пример.
db/seeds/object.yaml
# Реальный конфиг медиа-шлюза: RTSP-URL камер содержат логин/пароль; в репо — только пример.
media-gateway/go2rtc.yaml
# Приватные ключи лицензий вендора (#335) — секрет, в репозиторий не попадают.
licenses/

# IDE и ОС
.idea/
.vscode/
.DS_Store
Thumbs.db

# Готовые документы для заказчика (отрисованные из Markdown) — отслеживаем,
# несмотря на общий игнор build/ выше.
!docs/customer/build/
!docs/customer/build/*.pdf
!docs/customer/build/*.html

# Офлайн-бандл поставки заказчику (docker save + конфиги): большой артефакт,
# собирается scripts/build_offline_bundle.* в эту папку, в репозиторий не кладём.
/delivery/
